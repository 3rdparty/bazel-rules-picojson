load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "picojson",
    hdrs = [
        "picojson.h",
    ],
    includes = ["."], # NOTE: added so we can include with angle brackets.
    visibility = ["//visibility:public"],
)
